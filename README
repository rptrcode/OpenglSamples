First opengl sample
rotates a triangle on x/y axis.
Uses Object oriented principles for initializing opengl and showing window.


Issues:
window creation fails for any other class name
only this GLSample classname  works!!
	CreateWindow( "GLSample", "OpenGL Sample", dstyle, x, y, w, h, parent, NULL, hinst, NULL );

